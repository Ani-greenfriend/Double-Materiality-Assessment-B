VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Optional — Tool A's deployed site address, used only to build the full
# "Copy personal link" URL on Invitations (/survey/[slug]/[link_code]).
# Not in CLAUDE.md's Environment Variables list yet; add it there once the
# builder confirms Tool A's real domain. Without it, invitations still work
# — Copy Link falls back to copying just the /survey/... path.
VITE_TOOL_A_URL=
