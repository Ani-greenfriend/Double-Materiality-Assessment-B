VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Optional — overrides Tool A's site address (hardcoded default in
# src/lib/data.js: https://questionnaire-dma.netlify.app) for the
# "Copy personal link" URL on Invitations. Only needed if Tool A's domain
# changes.
VITE_TOOL_A_URL=
